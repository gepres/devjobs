<main class="contenedor">
  <form action="/editar-perfil" method="POST" class="default-form" enctype="multipart/form-data">
    <div class="campo">
      <label for="">Nombre</label>
      <input type="text" value="{{usuario.nombre}}" name="nombre" placeholder="Tu nombre" >
    </div>
    <div class="campo">
      <label for="">Email</label>
      <input type="email" value="{{usuario.email}}" name="email" placeholder="Tu Correo" >
    </div>
    <div class="campo">
      <label for="">Password</label>
      <input type="password" name="password" placeholder="Tu Contraseña">
    </div>
    <div class="campo">
      <label>Imagen</label>
      <input type="file" class="imagen" name="imagen" placeholder="Tu Imagen de perfil">
      <img src="uploads/perfiles/{{usuario.imagen}}" class="admin-perfil" alt="">
    </div>
    <div class="campo">
      <input type="submit" class="btn btn-azul" value="Guardar Cambios">
    </div>

  </form>
</main>